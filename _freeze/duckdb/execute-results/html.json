{
  "hash": "10e8e18997ef3fc592f355b077274f95",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"DuckDB for Marketing Analytics: From Parquet to Powerful Queries\"\nsubtitle: \"How to Combine DuckDB with Arrow + Parquet\"\ndescription: \"This page describes DuckDB and compares it with Arrow and Parquet and explain how to use them together effectively when dealing with big data\"\ncategories: [DuckDB, Arrow, Parquet]\norder: 2\nauthor: \"Jae Jung\"\ndate: today\nformat: \n    html:\n        theme: cosmo\n        toc: true\n        toc-depth: 3\n        toc-expand: 1\n        code-fold: show\n        toc-location: right-body\n        number-sections: true\n        #embed-resources: true\nexecute:\n  warning: false\n  message: false\n  echo: fenced\n  cache: true\n  freeze: auto\n---\n\n# Introduction\n\n## What is DuckDB?\n\nDuckDB is an embedded analytical database engine — think of it as \"SQLite, but built for analytics.\" It runs entirely inside your R process with no server, no installation, no configuration. You just install the package and it works.\n\nIt was designed from the ground up for **OLAP** (Online Analytical Processing) workloads — the kind of heavy aggregations, joins, and scans that are typical in data analysis, as opposed to transactional row-by-row operations.\n\n-   SQLite was built for this (transactional — many small reads/writes):\n    -   \"Insert one customer order\"\n    -   \"Update one user's address\"\n    -   \"Delete one session record\"\n-   DuckDB was built for this (analytical — few large scans):\n    -   \"What was total revenue by channel across 50 million events?\"\n    -   \"Which pages had the highest bounce rate last quarter?\"\n    -   \"Give me the 99th percentile session duration by device type\"\n\n## Three Remarkable Features\n\n### 1. It Lives Inside R — No Server Needed\n\n**Traditional database setup** (PostgreSQL, MySQL, Snowflake):\n\n1.  Install database server\n2.  Configure network/ports\n3.  Create users and permissions\n4.  Start the server process\n5.  Connect from R\n6.  Manage the server forever\n\n**DuckDB setup**:\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false         \ninstall.packages(\"duckdb\")\nlibrary(duckdb)\ncon <- dbConnect(duckdb())   # That's it. Running in 1 second.\n```\n````\n:::\n\n\n### 2. It Is Shockingly Fast\n\n> DuckDB uses columnar storage and vectorised execution — it reads only the columns your query needs and processes thousands of rows simultaneously using modern CPU instructions:\n\n**Benchmark:** Count events by type across 10 million GA4 rows\n\n-   data.frame in RAM: \\~4.2 seconds\n-   data.table: \\~0.8 seconds\n-   dplyr on tibble: \\~3.1 seconds\n-   DuckDB on parquet file: \\~0.3 seconds ← faster than data in RAM\n-   DuckDB on DuckDB file: \\~0.1 seconds\n\n### 3. It Reads Parquet Directly — Without Loading Into RAM\n\n> This is the feature that changes everything for large datasets:\n\n```{{r}}\n# Traditional approach — must fit in RAM\nga4_data <- arrow::read_parquet(\"data/ga4_events.parquet\")  # ~4 GB into RAM\nresult   <- ga4_data |> filter(...) |> summarise(...)        # then query\n\n# DuckDB approach — reads only what the query needs\ncon    <- dbConnect(duckdb::duckdb())\nresult <- dbGetQuery(con, \"\n  SELECT event_name, COUNT(*) as n\n  FROM read_parquet('data/ga4_events.parquet')\n  WHERE event_date >= '2021-01-01'\n  GROUP BY event_name\n\")\n# DuckDB scans only the event_name and event_date columns\n# Never loads price, device, geo, or any other column into RAM\n```\n\n## Why use DuckDB?\n\n1.  **It's fast.** DuckDB uses a columnar execution engine with vectorized query processing, meaning it processes chunks of columns at a time rather than row by row. For analytical queries on large data, this is dramatically faster than base R or even many database solutions.\n\n2.  **It's serverless.** No Docker, no Postgres instance, no DBA needed. It lives inside your session like any other R package.\n\n3.  **It speaks SQL fluently.** You can write standard SQL, or use it as a dplyr backend via dbplyr — so you don't need to learn a new API.\n\n4.  **It handles larger-than-memory data.** DuckDB can query files that don't fit in RAM by streaming and processing them in chunks, making it genuinely useful for big data workflows.\n\n5.  **It integrates natively with Parquet and Arrow.** It can directly query `.parquet` files on disk without loading them into memory first — no import step needed.\n\n## DuckDB vs Arrow vs Parquet\n\n> These three are complementary, not competing. Understanding the role of each is important:\n\n|   | **Parquet** | **Arrow** | **DuckDB** |\n|------------------|------------------|------------------|------------------|\n| What is it? | File format (How data is stored on disk) | In-memory format + compute engine (a toolkit): how data is held in RAM and processed | A query engine (how you ask questions of data) |\n| Lives where? | Disk | RAM | RAM + disk (processes from disk) |\n| Primary role | Store data efficiently | Process data in memory | Query data with SQL/dplyr |\n| Reads Parquet | IS Parquet | Yes — natively | Yes — directly |\n| SQL support | No | Limited (via DuckDB) | Full SQL |\n| dplyr support | Via arrow package | Yes — first class\" | Yes — via dbplyr |\n| Larger-than-memory? | Yes (it's on disk) | Partial (via chunked datasets) | Yes – streams from disk |\n| Query language | None | dplyr verbs | SQL + dplyr via dbplyr |\n| Speed (analytics) | N/A (Storage only) | Very fast | Extremely fast |\n| Best for | Archiving & sharing data | In-memory transformation: e.g., Wrangling in R/Python | Complex queries, joins, aggregations |\n\n> The typical mental model is:\n\n-   **Parquet** is where your data *lives* (on disk, compressed, efficient)\n\n-   **Arrow** is how you *move and process* it quickly in memory\n\n-   **DuckDB** is how you *query* it with full SQL power, with or without loading into memory\n\n::: callout-tip\nThey work beautifully together — DuckDB can read Arrow tables directly and query Parquet files on disk, and you can pass results back to Arrow or collect into R.\n:::\n\n# Setup: Connecting to BigQuery\n\n## Authentication and Connection\n\n::: callout-important\n## Before You Begin\n\nYou need a **Google Cloud Platform (GCP) account** with a project that has billing enabled. Querying the public dataset is free in terms of data costs (you won't be charged for the data itself), but GCP bills for query processing. Small queries on this dataset cost fractions of a cent.\n\n1.  Go to [console.cloud.google.com](https://console.cloud.google.com)\n2.  Create a project (or use an existing one)\n3.  Enable the BigQuery API for that project\n4.  Copy your **Project ID** — you'll need it below\n:::\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| label: connection\n#| eval: false\n\n# ── Replace with your GCP Project ID ─────────────────────────────────────────\n# Either set the environment variable: usethis::edit_r_environ()\n# and add: GCP_PROJECT_ID=your-actual-project-id\n# Or assign directly (less portable):\nbilling_project <- \"GCP_PROJECT_ID\"\n\n# ── Authenticate ──────────────────────────────────────────────────────────────\n# First run: opens a browser for OAuth. Subsequent runs: uses cached token.\n# In Positron IDE, if the browser redirect fails, add: use_oob = TRUE\nbq_auth()\n# bq_auth(use_oob = TRUE)  # Uncomment if you see an OAuth redirect error\n\n# ── Connect to the public GA4 dataset ─────────────────────────────────────────\ncon <- dbConnect(\n  bigrquery::bigquery(),\n  project = \"bigquery-public-data\",\n  dataset = \"ga4_obfuscated_sample_ecommerce\",\n  billing = billing_project\n)\n\n# Verify connection: list available tables\ndbListTables(con)\n```\n````\n:::\n\n\n## Downloading and Caching the Data\n\n> GA4 tables are large. For this workshop we download **one week of events** (\\~150,000 rows) once, then save locally to avoid re-querying BigQuery on every render. \n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| label: download-data\n#| eval: false\n\n# ── Run this chunk ONCE. It saves data locally. ───────────────────────────────\ndir.create(\"data\", showWarnings = FALSE)\n\nquery_result <- bq_project_query(\n  billing_project,\n  \"SELECT\n     event_date,\n     event_timestamp,\n     event_name,\n     event_params,\n     user_pseudo_id,\n     device,\n     geo,\n     traffic_source,\n     ecommerce,\n     items\n   FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*`\n   WHERE _TABLE_SUFFIX BETWEEN '20210101' AND '20210107'\" # 90 days of data\n)\n\n# A tibble data\nga4_raw <- query_result |>\n  bq_table_download(\n    bigint = \"numeric\", # Convert INT64 to R numeric (sufficient for most analyses)\n    page_size = 1000 # Download in batches of 10k rows\n  )\n```\n````\n:::\n\n\n:::callout-tip\nSince the data is relatively small size, we have three options to save the data as you can see below\n\n-   RDS, \n-   One Parquet file, \n-   Partitioned Parquet files.\n:::\n\n### Option 1: Saving it as RDS file\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| label: save-rds\n#| eval: false\nsaveRDS(ga4_raw, \"data/ga4_raw.rds\")\nmessage(\"Saved \", nrow(ga4_raw), \" rows to data/ga4_raw.rds\")\n```\n````\n:::\n\n\n> **Note**: RDS stands for R Data Serialization\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| label: load-rds\n#| eval: false\n\n# ── Load from local cache (use this for all subsequent analyses) ──────────────\nga4 <- readRDS(\"data/ga4_raw.rds\")\n```\n````\n:::\n\n\n### Option 2: Saving as a single Parquet file\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| label: save-parquet\n#| eval: false\n\nlibrary(arrow)\n\nwrite_parquet(ga4_raw, \"data/ga4_raw.parquet\")\nmessage(\"Saved to data/ga4_raw.parquet\")\n```\n````\n:::\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| label: load-parquet\n#| eval: false\n\nga4 <- read_parquet(\"data/ga4_raw.parquet\")\n```\n````\n:::\n\n\n### Options 3: Saving as Partitioned Parquet\n\n> Also no iteration needed — write_dataset() handles the partitioning automatically:\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| label: save-partitioned\n#| eval: false\n\nlibrary(arrow)\nlibrary(dplyr)\n\nga4_raw |>\n  group_by(event_date) |>\n  write_dataset(\"data/ga4_parquet\", format = \"parquet\")\n\nmessage(\"Saved partitioned Parquet to data/ga4_parquet/\")\n```\n````\n:::\n\n\n> **Note**: `write_dataset()` automatically creates one subfolder per `event_date`,\n> e.g. `data/ga4_parquet/event_date=20210101/part-0.parquet`\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| label: load-partitioned\n#| eval: false\n\nga4 <- open_dataset(\"data/ga4_parquet/\")\n```\n````\n:::\n\n\n# How DuckDB Works with Arrow and Parquet\n\n> There are **four integration patterns**, each suited to different situations:\n\n**Setup:**\n\n```{{r}}\nlibrary(duckdb)\nlibrary(arrow)\nlibrary(dplyr)\n```\n\n## Pattern 1. Query a Parquet file directly with DuckDB (no loading into R)\n\n-   The most direct approach — point DuckDB at a `Parquet` file and query it with SQL, no loading required:\n\n-   DuckDB can scan Parquet files on disk using `SQL` — nothing enters R memory until you `collect()`.\n\n### Simple:\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\n\nlibrary(duckdb)\nlibrary(DBI)\ncon <- dbConnect(duckdb())\n\n# Register and query a parquet file directly\ndbGetQuery(con, \"\n  SELECT region, SUM(sales) AS total_sales\n  FROM read_parquet('data/sales.parquet')\n  GROUP BY region\n  ORDER BY total_sales DESC\n\")\n```\n````\n:::\n\n\n### Advanced:\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\n\nlibrary(duckdb)\nlibrary(DBI)\ncon <- dbConnect(duckdb())\n\n# ── Query a single Parquet file ────────────────────────────────────────────────\ndbGetQuery(\n  con,\n  \"\n  SELECT\n    event_name,\n    COUNT(*) AS events,\n    COUNT(DISTINCT user_pseudo_id) AS users\n  FROM read_parquet('data/ga4_flat.parquet')\n  GROUP BY event_name\n  ORDER BY events DESC\n  LIMIT 10\n\"\n)\n\n# ── Query multiple Parquet files with a wildcard ───────────────────────────────\n# Perfect for GA4's one-file-per-day structure\ndbGetQuery(\n  con,\n  \"\n  SELECT\n    event_date,\n    COUNT(*) AS daily_events\n  FROM read_parquet('data/ga4_*.parquet')\n  GROUP BY event_date\n  ORDER BY event_date\n\"\n)\n\ndbGetQuery(\n  con,\n  \"\n  SELECT year, SUM(revenue) AS total\n  FROM read_parquet('data/sales/*.parquet', union_by_name = TRUE)\n  GROUP BY year\n  ORDER BY year\n\"\n\n# ── Join two Parquet files in a single query ───────────────────────────────────\ndbGetQuery(\n  con,\n  \"\n  SELECT\n    s.device_category,\n    s.traffic_source_medium,\n    COUNT(DISTINCT e.user_pseudo_id) AS users,\n    SUM(CASE WHEN e.event_name = 'purchase' THEN 1 ELSE 0 END) AS purchases\n  FROM read_parquet('data/ga4_flat.parquet')    AS e\n  JOIN read_parquet('data/ga4_sessions.parquet') AS s\n    ON e.user_pseudo_id = s.user_pseudo_id\n   AND e.event_date     = s.event_date\n  GROUP BY s.device_category, s.traffic_source_medium\n  ORDER BY users DESC\n\"\n)\n\ndbDisconnect(con, shutdown = TRUE)\n```\n````\n:::\n\n\n> This is powerful — DuckDB pushes the entire query down to the file, reading only the columns and rows it needs (thanks to Parquet's columnar layout).\n\n## Parttern 2. Use DuckDB as a dplyr backend (no SQL required)\n\n> If you prefer dplyr verbs over SQL, you can register the **Parquet file** as a **virtual table** and query it lazily.\n>\n> `dbplyr` translates dplyr verbs into DuckDB SQL automatically:\n\n### Simple:\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(duckdb)\nlibrary(dplyr)\nlibrary(dbplyr)\nlibrary(DBI)\n\ncon <- dbConnect(duckdb())\n\nduckdb_register(con, \"sales\", read_parquet(\"data/sales.parquet\"))\n\ntbl(con, \"sales\") |>\n  filter(year >= 2022) |>\n  group_by(region) |>\n  summarise(total = sum(sales)) |>\n  arrange(desc(total)) |>\n  collect()   # only now does data enter R\n```\n````\n:::\n\n\n### Advanced:\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(duckdb)\nlibrary(dplyr)\nlibrary(dbplyr)\nlibrary(DBI)\n\ncon <- dbConnect(duckdb())\n\n# ── Register Parquet file as a virtual table ───────────────────────────────────\n# This creates a view — no data is copied or loaded\nduckdb_read_csv   # (there is also this for CSV)\n\ndbExecute(con, \"\n  CREATE VIEW ga4_events AS\n  SELECT * FROM read_parquet('data/ga4_flat.parquet')\n\")\n\ndbExecute(con, \"\n  CREATE VIEW ga4_sessions AS\n  SELECT * FROM read_parquet('data/ga4_sessions.parquet')\n\")\n\n# ── Now use dplyr as if it were a regular data frame ──────────────────────────\nevents   <- tbl(con, \"ga4_events\")\nsessions <- tbl(con, \"ga4_sessions\")\n\n# dplyr query — translated to DuckDB SQL by dbplyr\ntop_events <- events |>\n  count(event_name, sort = TRUE) |>\n  head(10) |>\n  collect()    # collect() executes the query and returns a tibble\n\nprint(top_events)\n\n# Join two Parquet files using dplyr — no SQL written\ndevice_conversions <- sessions |>\n  select(user_pseudo_id, device_category, traffic_source_medium) |>\n  left_join(\n    events |>\n      filter(event_name == \"purchase\") |>\n      distinct(user_pseudo_id) |>\n      mutate(purchased = TRUE),\n    by = \"user_pseudo_id\"\n  ) |>\n  mutate(purchased = coalesce(purchased, FALSE)) |>\n  group_by(device_category, traffic_source_medium) |>\n  summarise(\n    users      = n_distinct(user_pseudo_id),\n    purchasers = sum(as.integer(purchased), na.rm = TRUE),\n    cvr        = purchasers / users,\n    .groups    = \"drop\"\n  ) |>\n  arrange(desc(cvr)) |>\n  collect()  # only now does data enter R\n\nprint(device_conversions)\n\ndbDisconnect(con, shutdown = TRUE)\n```\n````\n:::\n\n\n## Pattern 3. Query an Arrow Table with DuckDB (The Power Combination)\n\n> Arrow and DuckDB are designed to work together. Arrow handles file I/O and in-memory transformation; DuckDB handles querying.\n>\n> They share data without copying, using the Apache Arrow memory format.\n>\n> If you already have an Arrow table in memory, DuckDB can query it directly — zero copying involved:\n\n### Simple:\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\nlibrary(duckdb)\nlibrary(dplyr)\n\n# Create or load an Arrow table\narrow_table <- read_parquet(\"data/sales.parquet\", as_data_frame = FALSE)\n\ncon <- dbConnect(duckdb())\nduckdb_register_arrow(con, \"sales_arrow\", arrow_table)\n\ntbl(con, \"sales_arrow\") |>\n  filter(category == \"Electronics\") |>\n  summarise(avg_price = mean(price)) |>\n  collect()\n```\n````\n:::\n\n\n### Advanced:\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\n\nlibrary(arrow)\nlibrary(duckdb)\nlibrary(dplyr)\n\n# ── Step 1: Open Parquet as Arrow Dataset (lazy — nothing loaded yet) ──────────\nga4_arrow <- arrow::open_dataset(\"data/ga4_flat.parquet\")\n\n# ── Step 2: Do transformations in Arrow ───────────────────────────────────────\n# Arrow is better at: filtering rows, selecting columns, type conversion\nga4_prepared <- ga4_arrow |>\n  filter(event_name %in% c(\"purchase\", \"add_to_cart\", \"view_item\")) |>\n  select(event_date, event_name, user_pseudo_id) |>\n  mutate(event_date = as.Date(event_date))\n\n# ── Step 3: Hand off to DuckDB for aggregation ────────────────────────────────\n# DuckDB is better at: GROUP BY, window functions, complex joins\ncon <- dbConnect(duckdb())\n\n# Register the Arrow object directly — ZERO data copying\nduckdb_register_arrow(con, \"ga4_prepared\", ga4_prepared)\n\n# Now query it with full DuckDB SQL power\nresult <- dbGetQuery(con, \"\n  SELECT\n    event_date,\n    event_name,\n    COUNT(DISTINCT user_pseudo_id) AS users\n  FROM ga4_prepared\n  GROUP BY event_date, event_name\n  ORDER BY event_date, event_name\n\")\n\nprint(result)\ndbDisconnect(con, shutdown = TRUE)\n\n# ── The same workflow using dplyr throughout ───────────────────────────────────\nlibrary(arrow)\nlibrary(duckdb)\nlibrary(dplyr)\n\ncon <- dbConnect(duckdb::duckdb())\n\n# Arrow dataset → register with DuckDB → query with dplyr\nga4_arrow <- arrow::open_dataset(\"data/ga4_flat.parquet\")\nduckdb::duckdb_register_arrow(con, \"ga4_events\", ga4_arrow)\n\nga4_params_arrow <- arrow::open_dataset(\"data/ga4_event_params.parquet\")\nduckdb::duckdb_register_arrow(con, \"ga4_params\", ga4_params_arrow)\n\n# Now use pure dplyr — no SQL needed at all\n# dbplyr translates these verbs to DuckDB SQL automatically\n\n# Marketing analysis: page views by device and channel\npage_analysis <- tbl(con, \"ga4_params\") |>\n  filter(key == \"page_location\") |>\n  rename(page_location = param_value) |>\n  left_join(\n    tbl(con, \"ga4_events\") |>\n      select(event_timestamp, user_pseudo_id,\n             device_category, traffic_source_medium),\n    by = c(\"event_timestamp\", \"user_pseudo_id\")\n  ) |>\n  mutate(\n    page_path = regexp_replace(page_location, \"\\\\?.*$\", \"\"),\n    page_path = regexp_replace(page_path, \"https?://[^/]+\", \"\")\n  ) |>\n  group_by(device_category, page_path) |>\n  summarise(\n    page_views = n(),\n    users      = n_distinct(user_pseudo_id),\n    .groups    = \"drop\"\n  ) |>\n  filter(page_views >= 10) |>\n  arrange(desc(page_views)) |>\n  head(20) |>\n  collect()\n\nprint(page_analysis)\ndbDisconnect(con, shutdown = TRUE)\n```\n````\n:::\n\n\n## Pattern 4. Persistent DuckDB Database (Best for a Course Project)\n\n> Write your rectangled GA4 tables once into a `.duckdb` file. Every subsequent analysis opens this one file — no BigQuery, no Parquet management, no RAM issues:\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\n\nlibrary(duckdb)\nlibrary(dplyr)\nlibrary(arrow)\n\n# ── SETUP: Run once to create the database ────────────────────────────────────\ncon <- dbConnect(\n  duckdb::duckdb(),\n  dbdir     = \"data/ga4.duckdb\",\n  read_only = FALSE\n)\n\n# Write tables from Parquet files — DuckDB reads and stores them\ndbExecute(con, \"\n  CREATE TABLE ga4_events AS\n  SELECT * FROM read_parquet('data/ga4_flat.parquet')\n\")\n\ndbExecute(con, \"\n  CREATE TABLE ga4_params AS\n  SELECT * FROM read_parquet('data/ga4_event_params.parquet')\n\")\n\ndbExecute(con, \"\n  CREATE TABLE ga4_sessions AS\n  SELECT * FROM read_parquet('data/ga4_sessions.parquet')\n\")\n\ndbExecute(con, \"\n  CREATE TABLE ga4_items AS\n  SELECT * FROM read_parquet('data/ga4_items.parquet')\n\")\n\n# Add indexes for faster querying\ndbExecute(con, \"CREATE INDEX idx_event_name ON ga4_events(event_name)\")\ndbExecute(con, \"CREATE INDEX idx_param_key  ON ga4_params(key)\")\ndbExecute(con, \"CREATE INDEX idx_event_date ON ga4_events(event_date)\")\n\n# Confirm what was saved\ncat(\"Tables in ga4.duckdb:\\n\")\ndbListTables(con) |> print()\n\ncat(\"\\nRow counts:\\n\")\npurrr::map_df(dbListTables(con), \\(t) {\n  tibble::tibble(\n    table = t,\n    rows  = dbGetQuery(con,\n              glue::glue(\"SELECT COUNT(*) n FROM {t}\"))$n\n  )\n}) |> print()\n\ncat(\"\\nFile size:\",\n    round(file.size(\"data/ga4.duckdb\") / 1024^2, 1), \"MB\\n\")\n\ndbDisconnect(con, shutdown = TRUE)\n```\n````\n:::\n\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\n\n# ── ANALYSIS: Open the database for every subsequent session ──────────────────\ncon <- dbConnect(\n  duckdb::duckdb(),\n  dbdir     = \"data/ga4.duckdb\",\n  read_only = TRUE    # Prevents accidental writes during analysis\n)\n\n# Lazy table references — nothing in RAM yet\nevents   <- tbl(con, \"ga4_events\")\nparams   <- tbl(con, \"ga4_params\")\nsessions <- tbl(con, \"ga4_sessions\")\nitems    <- tbl(con, \"ga4_items\")\n\n# ── Example 1: Event counts (dplyr) ───────────────────────────────────────────\nevents |>\n  count(event_name, sort = TRUE) |>\n  collect()\n\n# ── Example 2: Top pages (dplyr) ──────────────────────────────────────────────\nparams |>\n  filter(key == \"page_location\") |>\n  count(param_value, sort = TRUE) |>\n  head(10) |>\n  collect()\n\n# ── Example 3: Revenue by day (SQL) ───────────────────────────────────────────\ndbGetQuery(con, \"\n  SELECT\n    event_date,\n    COUNT(*)                                   AS transactions,\n    SUM(ecommerce_purchase_revenue_in_usd)     AS revenue,\n    AVG(ecommerce_purchase_revenue_in_usd)     AS avg_order_value\n  FROM ga4_events\n  WHERE event_name = 'purchase'\n  GROUP BY event_date\n  ORDER BY event_date\n\")\n\n# ── Example 4: Funnel analysis (SQL window functions) ─────────────────────────\ndbGetQuery(con, \"\n  WITH funnel AS (\n    SELECT\n      event_name,\n      COUNT(DISTINCT user_pseudo_id) AS users,\n      ROW_NUMBER() OVER (\n        ORDER BY CASE event_name\n          WHEN 'view_item'       THEN 1\n          WHEN 'add_to_cart'     THEN 2\n          WHEN 'begin_checkout'  THEN 3\n          WHEN 'purchase'        THEN 4\n        END\n      ) AS stage_order\n    FROM ga4_events\n    WHERE event_name IN ('view_item','add_to_cart',\n                         'begin_checkout','purchase')\n    GROUP BY event_name\n  )\n  SELECT\n    event_name,\n    users,\n    ROUND(100.0 * users /\n      FIRST_VALUE(users) OVER (ORDER BY stage_order), 1) AS pct_of_top\n  FROM funnel\n  ORDER BY stage_order\n\")\n\ndbDisconnect(con, shutdown = TRUE)\n```\n````\n:::\n\n\n# How the Three Tools Form a Complete Stack\n\n```         \n# The complete GA4 analytics stack:\n\n# ┌─────────────────────────────────────────────────────────────┐\n# │                     YOUR R SESSION                          │\n# │                                                             │\n# │  ┌──────────┐    ┌──────────┐    ┌──────────────────────┐  │\n# │  │  Arrow   │    │  dplyr   │    │        ggplot2        │  │\n# │  │          │    │  dbplyr  │    │        gt             │  │\n# │  │ Read &   │    │          │    │                       │  │\n# │  │ Write    │    │ Verbs →  │    │  Visualise & Report   │  │\n# │  │ Parquet  │    │ SQL      │    │                       │  │\n# │  └────┬─────┘    └────┬─────┘    └───────────┬──────────┘  │\n# │       │               │                       │             │\n# │       └───────────────▼───────────────────────┘             │\n# │                  ┌──────────┐                               │\n# │                  │  DuckDB  │                               │\n# │                  │          │                               │\n# │                  │  Query   │                               │\n# │                  │  Engine  │                               │\n# │                  └────┬─────┘                               │\n# └───────────────────────┼─────────────────────────────────────┘\n#                         │\n#          ┌──────────────┼──────────────┐\n#          │              │              │\n#    ┌─────▼──────┐ ┌─────▼──────┐ ┌───▼────────────┐\n#    │  Parquet   │ │  Parquet   │ │  ga4.duckdb    │\n#    │  ga4_flat  │ │  ga4_params│ │  (all tables)  │\n#    └────────────┘ └────────────┘ └────────────────┘\n#         DISK            DISK           DISK\n\n# Arrow   = the loading dock (moves data efficiently)\n# DuckDB  = the analyst (asks and answers questions)\n# Parquet = the filing system (stores data efficiently)\n```\n\n# The Big Picture Workflow\n\n\n::: {.cell}\n::: {.cell-output-display}\n\n```{=html}\n<div class=\"grViz html-widget html-fill-item\" id=\"htmlwidget-b56069e3ebba8a05dc20\" style=\"width:100%;height:464px;\"></div>\n<script type=\"application/json\" data-for=\"htmlwidget-b56069e3ebba8a05dc20\">{\"x\":{\"diagram\":\"\\n  digraph workflow {\\n    graph [layout = dot, rankdir = TD]\\n    node [shape = rectangle, style = filled]\\n    \\n    A [label = \\\"Parquet files on disk\\\", fillcolor = \\\"#C0DD97\\\"]\\n    B [label = \\\"DuckDB queries\\\", fillcolor = \\\"#B5D4F4\\\"]\\n    C [label = \\\"Large intermediate result?\\\", fillcolor = \\\"#FAC775\\\", shape = diamond]\\n    D [label = \\\"compute()\\\", fillcolor = \\\"#B5D4F4\\\"]\\n    E [label = \\\"collect()\\\", fillcolor = \\\"#B5D4F4\\\"]\\n    F [label = \\\"Visualize - ggplot2\\\", fillcolor = \\\"#F4C0D1\\\"]\\n    G [label = \\\"Model - tidymodels\\\", fillcolor = \\\"#F4C0D1\\\"]\\n    \\n    A -> B -> C\\n    C -> D [label = \\\"Yes\\\"]\\n    C -> E [label = \\\"No\\\"]\\n    D -> E -> F\\n    E -> G\\n  }\\n\",\"config\":{\"engine\":\"dot\",\"options\":null}},\"evals\":[],\"jsHooks\":[]}</script>\n```\n\n:::\n:::\n\n\n**Parquet is your warehouse, Arrow is your forklift, DuckDB is your analyst.** Each does its job, and they hand off to each other seamlessly.\n\n# `open_dataset` vs. `read_parquet` functions\n\n## The core difference\n\n> `open_dataset()` is for **many files or a folder** — it creates a lazy connection without loading anything into memory. `read_parquet()` is for **a single file** — it reads the file directly, by default into R memory.\n>\n> **The One-Sentence Description**:\n\n```{{r}}\n# read_parquet()  — loads data INTO RAM immediately, gives you a tibble\n# open_dataset()  — creates a POINTER to data on disk, loads nothing yet\n```\n\n\\> Everything else flows from this single difference. \\> \\> **What Each Function Actually Does**:\n\n## `read_parquet()` — Eager Loading\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\n\n# read_parquet() works like read.csv() or readRDS()\n# It reads the file immediately and returns a tibble in memory\n\nga4_data <- arrow::read_parquet(\"data/ga4_flat.parquet\")\n\n# After this line:\n# ✓ All data is in RAM\n# ✓ ga4_data is a regular tibble\n# ✓ You can use it without Arrow at all — it's just a data frame\n# ✗ If the file is larger than RAM — this fails or crashes R\n\nclass(ga4_data)\n#> [1] \"tbl_df\"     \"tbl\"        \"data.frame\"\n# ← a regular tibble, no Arrow in sight after loading\n\nobject.size(ga4_data) |> format(units = \"MB\")\n#> \"187.4 MB\"   ← sitting in your RAM right now\n```\n````\n:::\n\n\n## `open_dataset()` — Lazy Loading\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\n\n# open_dataset() works like a database connection\n# It reads the file METADATA only and returns an Arrow Dataset object\n# No actual data rows are loaded until you explicitly ask for them\n\nga4_dataset <- arrow::open_dataset(\"data/ga4_flat.parquet\")\n\n# After this line:\n# ✓ Metadata is read (schema, number of rows, column types)\n# ✓ You can query it immediately with dplyr verbs\n# ✓ Works on files larger than RAM\n# ✗ ga4_dataset is NOT a tibble — it is an Arrow Dataset object\n\nclass(ga4_dataset)\n#> [1] \"FileSystemDataset\" \"Dataset\" \"ArrowObject\" \"R6\"\n# ← an Arrow Dataset — NOT a data frame yet\n\nobject.size(ga4_dataset) |> format(units = \"KB\")\n#> \"4.2 KB\"    ← almost nothing in RAM — just the metadata pointer\n```\n````\n:::\n\n\n## Side-by-Side Comparison\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\nlibrary(dplyr)\n\n# ── Same question asked both ways ─────────────────────────────────────────────\n# \"What are the top 5 event types by count?\"\n\n# ── read_parquet() approach ───────────────────────────────────────────────────\n# Step 1: Load EVERYTHING into RAM (even columns we don't need)\nga4_tibble <- read_parquet(\"data/ga4_flat.parquet\")\n# Step 2: Then filter in R\nga4_tibble |>\n  count(event_name, sort = TRUE) |>\n  slice_head(n = 5)\n\n# ── open_dataset() approach ───────────────────────────────────────────────────\n# Step 1: Open a pointer (nothing loaded)\nga4_ds <- open_dataset(\"data/ga4_flat.parquet\")\n# Step 2: Build a query (still nothing loaded)\nquery <- ga4_ds |>\n  count(event_name, sort = TRUE) |>\n  slice_head(n = 5)\n# Step 3: Execute — Arrow reads ONLY the event_name column\nquery |> collect()\n\n# Both produce identical results\n# But open_dataset() used a fraction of the RAM\n```\n````\n:::\n\n\n## The Key Concept: Lazy vs Eager Evaluation\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\nlibrary(dplyr)\n\nga4_ds <- open_dataset(\"data/ga4_flat.parquet\")\n\n# ── Every dplyr verb on an Arrow Dataset is LAZY ─────────────────────────────\n# Nothing happens when you call these — they just build a query plan\n\nstep1 <- ga4_ds |> filter(event_name == \"purchase\")\nstep2 <- step1  |> select(event_date, user_pseudo_id,\n                            ecommerce_purchase_revenue_in_usd)\nstep3 <- step2  |> group_by(event_date)\nstep4 <- step3  |> summarise(revenue = sum(ecommerce_purchase_revenue_in_usd,\n                                            na.rm = TRUE))\n\n# At this point: ZERO rows have been read from disk\n# step4 is just a description of what to do\n\n# ── collect() EXECUTES the query ─────────────────────────────────────────────\n# Only now does Arrow read from disk — and only the columns it needs\nresult <- step4 |> collect()\n\n# Arrow read: event_name, event_date, user_pseudo_id,\n#             ecommerce_purchase_revenue_in_usd\n# Arrow skipped: every other column in the file\n# This is called \"column pruning\" — a massive RAM saving\n\n# ── You can inspect the query plan before executing ───────────────────────────\nga4_ds |>\n  filter(event_name == \"purchase\") |>\n  group_by(event_date) |>\n  summarise(revenue = sum(ecommerce_purchase_revenue_in_usd,\n                          na.rm = TRUE)) |>\n  explain()    # Shows Arrow's execution plan — no data read yet\n```\n````\n:::\n\n\n## The Biggest Difference: Multiple Files\n\nThis is where `open_dataset()` becomes **dramatically** more powerful.\n`read_parquet()` reads one file. `open_dataset()` reads many files as\nif they were one:\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\n\n# ── Scenario: GA4 data saved as one Parquet file per day ─────────────────────\n# data/\n#   ga4_20201101.parquet\n#   ga4_20201102.parquet\n#   ga4_20201103.parquet\n#   ... (365 files)\n\n# ── read_parquet(): must read files one at a time ─────────────────────────────\nlibrary(purrr)\n\n# Cumbersome — load all files and bind them\nall_files <- list.files(\"data\", pattern = \"ga4_\\\\d{8}.parquet\",\n                         full.names = TRUE)\n\nga4_all <- all_files |>\n  map(\\(f) read_parquet(f)) |>\n  list_rbind()\n# Loads ALL files into RAM simultaneously\n# If 365 files × 50 MB each = 18 GB — crashes R\n\n# ── open_dataset(): treats ALL files as one dataset ──────────────────────────\nga4_all <- open_dataset(\"data/\")   # Point at the folder, not a file\n# OR be more specific:\nga4_all <- open_dataset(\n  sources = list.files(\"data\", pattern = \"ga4_\\\\d{8}.parquet\",\n                        full.names = TRUE)\n)\n\n# Now query across ALL 365 days as if it were one table\n# Arrow reads only the files and columns needed to answer your query\nga4_all |>\n  filter(event_date >= \"2021-01-01\",\n         event_date <= \"2021-01-31\") |>\n  count(event_name) |>\n  collect()\n# Arrow only opens January files — skips the other 334 files entirely\n```\n````\n:::\n\n\n## Hive-Partitioned Datasets — The Most Powerful Pattern\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\nlibrary(dplyr)\n\n# ── Save GA4 data in Hive partition format ────────────────────────────────────\n# Hive partitioning stores data in folders named key=value\n# This lets open_dataset() skip entire folders based on filter conditions\n\nga4_tibble <- readRDS(\"data/ga4_raw.rds\") |>\n  mutate(\n    year  = lubridate::year(lubridate::ymd(event_date)),\n    month = lubridate::month(lubridate::ymd(event_date))\n  )\n\n# Write partitioned dataset\narrow::write_dataset(\n  ga4_tibble,\n  path             = \"data/ga4_partitioned\",\n  format           = \"parquet\",\n  partitioning     = c(\"year\", \"month\"),   # Partition by year and month\n  compression      = \"snappy\"\n)\n\n# Creates this folder structure:\n# data/ga4_partitioned/\n#   year=2020/\n#     month=11/\n#       part-0.parquet\n#     month=12/\n#       part-0.parquet\n#   year=2021/\n#     month=1/\n#       part-0.parquet\n#     month=2/\n#       part-0.parquet\n\n# ── open_dataset() understands Hive partitioning automatically ────────────────\nga4_partitioned <- open_dataset(\n  \"data/ga4_partitioned\",\n  partitioning = c(\"year\", \"month\")   # Tell Arrow how it is partitioned\n)\n\n# Query January 2021 — Arrow reads ONLY year=2021/month=1/ folder\njan_2021 <- ga4_partitioned |>\n  filter(year == 2021, month == 1) |>\n  count(event_name) |>\n  collect()\n\n# Arrow skipped every other year= and month= folder\n# This is called \"partition pruning\" — can be 100x faster on large datasets\n\n# ── Verify the schema includes partition columns ──────────────────────────────\nga4_partitioned$schema\n#> Schema\n#> event_date: string\n#> event_name: string\n#> user_pseudo_id: string\n#> ...\n#> year: int32      ← added automatically from folder name\n#> month: int32     ← added automatically from folder name\n```\n````\n:::\n\n\n## Comparison table\n\n|   | `open_dataset()` | `read_parquet()` |\n|------------------------|------------------------|------------------------|\n| Input | Folder, multiple files, or single file | Single file only |\n| Loads into memory? | No — lazy connection | Yes by default |\n| Returns | Arrow Dataset object | R dataframe or Arrow Table |\n| Partitioned folders? | Yes, auto-detects partitions | No |\n| Multiple file formats? | Yes (Parquet, CSV, Arrow) | Parquet only |\n| Use with dplyr? | Yes, lazily | Yes, but already in memory |\n| Best for | Large, partitioned, multi-file data | Small-to-medium single files |\n\n\n### Detailed Feature Comparison Table\n\n\n::: {.cell}\n::: {.cell-output-display}\n\n```{=html}\n<div id=\"uyefdtrcgt\" style=\"padding-left:0px;padding-right:0px;padding-top:10px;padding-bottom:10px;overflow-x:auto;overflow-y:auto;width:auto;height:auto;\">\n<style>#uyefdtrcgt table {\n  font-family: system-ui, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol', 'Noto Color Emoji';\n  -webkit-font-smoothing: antialiased;\n  -moz-osx-font-smoothing: grayscale;\n}\n\n#uyefdtrcgt thead, #uyefdtrcgt tbody, #uyefdtrcgt tfoot, #uyefdtrcgt tr, #uyefdtrcgt td, #uyefdtrcgt th {\n  border-style: none;\n}\n\n#uyefdtrcgt p {\n  margin: 0;\n  padding: 0;\n}\n\n#uyefdtrcgt .gt_table {\n  display: table;\n  border-collapse: collapse;\n  line-height: normal;\n  margin-left: auto;\n  margin-right: auto;\n  color: #333333;\n  font-size: 16px;\n  font-weight: normal;\n  font-style: normal;\n  background-color: #FFFFFF;\n  width: auto;\n  border-top-style: solid;\n  border-top-width: 2px;\n  border-top-color: #004D80;\n  border-right-style: none;\n  border-right-width: 2px;\n  border-right-color: #D3D3D3;\n  border-bottom-style: solid;\n  border-bottom-width: 2px;\n  border-bottom-color: #004D80;\n  border-left-style: none;\n  border-left-width: 2px;\n  border-left-color: #D3D3D3;\n}\n\n#uyefdtrcgt .gt_caption {\n  padding-top: 4px;\n  padding-bottom: 4px;\n}\n\n#uyefdtrcgt .gt_title {\n  color: #333333;\n  font-size: 125%;\n  font-weight: initial;\n  padding-top: 4px;\n  padding-bottom: 4px;\n  padding-left: 5px;\n  padding-right: 5px;\n  border-bottom-color: #FFFFFF;\n  border-bottom-width: 0;\n}\n\n#uyefdtrcgt .gt_subtitle {\n  color: #333333;\n  font-size: 85%;\n  font-weight: initial;\n  padding-top: 3px;\n  padding-bottom: 5px;\n  padding-left: 5px;\n  padding-right: 5px;\n  border-top-color: #FFFFFF;\n  border-top-width: 0;\n}\n\n#uyefdtrcgt .gt_heading {\n  background-color: #FFFFFF;\n  text-align: center;\n  border-bottom-color: #FFFFFF;\n  border-left-style: none;\n  border-left-width: 1px;\n  border-left-color: #D3D3D3;\n  border-right-style: none;\n  border-right-width: 1px;\n  border-right-color: #D3D3D3;\n}\n\n#uyefdtrcgt .gt_bottom_border {\n  border-bottom-style: solid;\n  border-bottom-width: 2px;\n  border-bottom-color: #0076BA;\n}\n\n#uyefdtrcgt .gt_col_headings {\n  border-top-style: solid;\n  border-top-width: 2px;\n  border-top-color: #0076BA;\n  border-bottom-style: solid;\n  border-bottom-width: 2px;\n  border-bottom-color: #0076BA;\n  border-left-style: none;\n  border-left-width: 1px;\n  border-left-color: #D3D3D3;\n  border-right-style: none;\n  border-right-width: 1px;\n  border-right-color: #D3D3D3;\n}\n\n#uyefdtrcgt .gt_col_heading {\n  color: #333333;\n  background-color: #FFFFFF;\n  font-size: 100%;\n  font-weight: normal;\n  text-transform: inherit;\n  border-left-style: none;\n  border-left-width: 1px;\n  border-left-color: #D3D3D3;\n  border-right-style: none;\n  border-right-width: 1px;\n  border-right-color: #D3D3D3;\n  vertical-align: bottom;\n  padding-top: 5px;\n  padding-bottom: 6px;\n  padding-left: 5px;\n  padding-right: 5px;\n  overflow-x: hidden;\n}\n\n#uyefdtrcgt .gt_column_spanner_outer {\n  color: #333333;\n  background-color: #FFFFFF;\n  font-size: 100%;\n  font-weight: normal;\n  text-transform: inherit;\n  padding-top: 0;\n  padding-bottom: 0;\n  padding-left: 4px;\n  padding-right: 4px;\n}\n\n#uyefdtrcgt .gt_column_spanner_outer:first-child {\n  padding-left: 0;\n}\n\n#uyefdtrcgt .gt_column_spanner_outer:last-child {\n  padding-right: 0;\n}\n\n#uyefdtrcgt .gt_column_spanner {\n  border-bottom-style: solid;\n  border-bottom-width: 2px;\n  border-bottom-color: #0076BA;\n  vertical-align: bottom;\n  padding-top: 5px;\n  padding-bottom: 5px;\n  overflow-x: hidden;\n  display: inline-block;\n  width: 100%;\n}\n\n#uyefdtrcgt .gt_spanner_row {\n  border-bottom-style: hidden;\n}\n\n#uyefdtrcgt .gt_group_heading {\n  padding-top: 8px;\n  padding-bottom: 8px;\n  padding-left: 5px;\n  padding-right: 5px;\n  color: #333333;\n  background-color: #FFFFFF;\n  font-size: 100%;\n  font-weight: initial;\n  text-transform: inherit;\n  border-top-style: solid;\n  border-top-width: 2px;\n  border-top-color: #0076BA;\n  border-bottom-style: solid;\n  border-bottom-width: 2px;\n  border-bottom-color: #0076BA;\n  border-left-style: none;\n  border-left-width: 1px;\n  border-left-color: #D3D3D3;\n  border-right-style: none;\n  border-right-width: 1px;\n  border-right-color: #D3D3D3;\n  vertical-align: middle;\n  text-align: left;\n}\n\n#uyefdtrcgt .gt_empty_group_heading {\n  padding: 0.5px;\n  color: #333333;\n  background-color: #FFFFFF;\n  font-size: 100%;\n  font-weight: initial;\n  border-top-style: solid;\n  border-top-width: 2px;\n  border-top-color: #0076BA;\n  border-bottom-style: solid;\n  border-bottom-width: 2px;\n  border-bottom-color: #0076BA;\n  vertical-align: middle;\n}\n\n#uyefdtrcgt .gt_from_md > :first-child {\n  margin-top: 0;\n}\n\n#uyefdtrcgt .gt_from_md > :last-child {\n  margin-bottom: 0;\n}\n\n#uyefdtrcgt .gt_row {\n  padding-top: 8px;\n  padding-bottom: 8px;\n  padding-left: 5px;\n  padding-right: 5px;\n  margin: 10px;\n  border-top-style: none;\n  border-top-width: 1px;\n  border-top-color: #89D3FE;\n  border-left-style: none;\n  border-left-width: 1px;\n  border-left-color: #89D3FE;\n  border-right-style: none;\n  border-right-width: 1px;\n  border-right-color: #89D3FE;\n  vertical-align: middle;\n  overflow-x: hidden;\n}\n\n#uyefdtrcgt .gt_stub {\n  color: #FFFFFF;\n  background-color: #0076BA;\n  font-size: 100%;\n  font-weight: initial;\n  text-transform: inherit;\n  border-right-style: solid;\n  border-right-width: 2px;\n  border-right-color: #0076BA;\n  padding-left: 5px;\n  padding-right: 5px;\n}\n\n#uyefdtrcgt .gt_stub_row_group {\n  color: #333333;\n  background-color: #FFFFFF;\n  font-size: 100%;\n  font-weight: initial;\n  text-transform: inherit;\n  border-right-style: solid;\n  border-right-width: 2px;\n  border-right-color: #D3D3D3;\n  padding-left: 5px;\n  padding-right: 5px;\n  vertical-align: top;\n}\n\n#uyefdtrcgt .gt_row_group_first td {\n  border-top-width: 2px;\n}\n\n#uyefdtrcgt .gt_row_group_first th {\n  border-top-width: 2px;\n}\n\n#uyefdtrcgt .gt_summary_row {\n  color: #333333;\n  background-color: #FFFFFF;\n  text-transform: inherit;\n  padding-top: 8px;\n  padding-bottom: 8px;\n  padding-left: 5px;\n  padding-right: 5px;\n}\n\n#uyefdtrcgt .gt_first_summary_row {\n  border-top-style: solid;\n  border-top-color: #0076BA;\n}\n\n#uyefdtrcgt .gt_first_summary_row.thick {\n  border-top-width: 2px;\n}\n\n#uyefdtrcgt .gt_last_summary_row {\n  padding-top: 8px;\n  padding-bottom: 8px;\n  padding-left: 5px;\n  padding-right: 5px;\n  border-bottom-style: solid;\n  border-bottom-width: 2px;\n  border-bottom-color: #0076BA;\n}\n\n#uyefdtrcgt .gt_grand_summary_row {\n  color: #333333;\n  background-color: #89D3FE;\n  text-transform: inherit;\n  padding-top: 8px;\n  padding-bottom: 8px;\n  padding-left: 5px;\n  padding-right: 5px;\n}\n\n#uyefdtrcgt .gt_first_grand_summary_row {\n  padding-top: 8px;\n  padding-bottom: 8px;\n  padding-left: 5px;\n  padding-right: 5px;\n  border-top-style: double;\n  border-top-width: 6px;\n  border-top-color: #0076BA;\n}\n\n#uyefdtrcgt .gt_last_grand_summary_row_top {\n  padding-top: 8px;\n  padding-bottom: 8px;\n  padding-left: 5px;\n  padding-right: 5px;\n  border-bottom-style: double;\n  border-bottom-width: 6px;\n  border-bottom-color: #0076BA;\n}\n\n#uyefdtrcgt .gt_striped {\n  background-color: #F4F4F4;\n}\n\n#uyefdtrcgt .gt_table_body {\n  border-top-style: solid;\n  border-top-width: 2px;\n  border-top-color: #0076BA;\n  border-bottom-style: solid;\n  border-bottom-width: 2px;\n  border-bottom-color: #0076BA;\n}\n\n#uyefdtrcgt .gt_footnotes {\n  color: #333333;\n  background-color: #FFFFFF;\n  border-bottom-style: none;\n  border-bottom-width: 2px;\n  border-bottom-color: #D3D3D3;\n  border-left-style: none;\n  border-left-width: 2px;\n  border-left-color: #D3D3D3;\n  border-right-style: none;\n  border-right-width: 2px;\n  border-right-color: #D3D3D3;\n}\n\n#uyefdtrcgt .gt_footnote {\n  margin: 0px;\n  font-size: 90%;\n  padding-top: 4px;\n  padding-bottom: 4px;\n  padding-left: 5px;\n  padding-right: 5px;\n}\n\n#uyefdtrcgt .gt_sourcenotes {\n  color: #333333;\n  background-color: #FFFFFF;\n  border-bottom-style: none;\n  border-bottom-width: 2px;\n  border-bottom-color: #D3D3D3;\n  border-left-style: none;\n  border-left-width: 2px;\n  border-left-color: #D3D3D3;\n  border-right-style: none;\n  border-right-width: 2px;\n  border-right-color: #D3D3D3;\n}\n\n#uyefdtrcgt .gt_sourcenote {\n  font-size: 90%;\n  padding-top: 4px;\n  padding-bottom: 4px;\n  padding-left: 5px;\n  padding-right: 5px;\n}\n\n#uyefdtrcgt .gt_left {\n  text-align: left;\n}\n\n#uyefdtrcgt .gt_center {\n  text-align: center;\n}\n\n#uyefdtrcgt .gt_right {\n  text-align: right;\n  font-variant-numeric: tabular-nums;\n}\n\n#uyefdtrcgt .gt_font_normal {\n  font-weight: normal;\n}\n\n#uyefdtrcgt .gt_font_bold {\n  font-weight: bold;\n}\n\n#uyefdtrcgt .gt_font_italic {\n  font-style: italic;\n}\n\n#uyefdtrcgt .gt_super {\n  font-size: 65%;\n}\n\n#uyefdtrcgt .gt_footnote_marks {\n  font-size: 75%;\n  vertical-align: 0.4em;\n  position: initial;\n}\n\n#uyefdtrcgt .gt_asterisk {\n  font-size: 100%;\n  vertical-align: 0;\n}\n\n#uyefdtrcgt .gt_indent_1 {\n  text-indent: 5px;\n}\n\n#uyefdtrcgt .gt_indent_2 {\n  text-indent: 10px;\n}\n\n#uyefdtrcgt .gt_indent_3 {\n  text-indent: 15px;\n}\n\n#uyefdtrcgt .gt_indent_4 {\n  text-indent: 20px;\n}\n\n#uyefdtrcgt .gt_indent_5 {\n  text-indent: 25px;\n}\n\n#uyefdtrcgt .katex-display {\n  display: inline-flex !important;\n  margin-bottom: 0.75em !important;\n}\n\n#uyefdtrcgt div.Reactable > div.rt-table > div.rt-thead > div.rt-tr.rt-tr-group-header > div.rt-th-group:after {\n  height: 0px !important;\n}\n</style>\n<table class=\"gt_table\" data-quarto-disable-processing=\"false\" data-quarto-bootstrap=\"false\">\n  <thead>\n    <tr class=\"gt_heading\">\n      <td colspan=\"3\" class=\"gt_heading gt_title gt_font_normal\" style>read_parquet() vs open_dataset()</td>\n    </tr>\n    <tr class=\"gt_heading\">\n      <td colspan=\"3\" class=\"gt_heading gt_subtitle gt_font_normal gt_bottom_border\" style>Complete feature comparison</td>\n    </tr>\n    <tr class=\"gt_col_headings\">\n      <th class=\"gt_col_heading gt_columns_bottom_border gt_left\" rowspan=\"1\" colspan=\"1\" style=\"font-weight: bold;\" scope=\"col\" id=\"Feature\">Feature</th>\n      <th class=\"gt_col_heading gt_columns_bottom_border gt_left\" rowspan=\"1\" colspan=\"1\" style=\"font-weight: bold;\" scope=\"col\" id=\"read_parquet\">read_parquet()</th>\n      <th class=\"gt_col_heading gt_columns_bottom_border gt_left\" rowspan=\"1\" colspan=\"1\" style=\"font-weight: bold;\" scope=\"col\" id=\"open_dataset\">open_dataset()</th>\n    </tr>\n  </thead>\n  <tbody class=\"gt_table_body\">\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left\">Returns</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left\">tibble (in RAM)</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left\">Arrow Dataset (pointer)</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left gt_striped\">Data loaded immediately</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left gt_striped\">Yes — all of it</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left gt_striped\">No — metadata only</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left\">Works on single file</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left\">Yes</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left\">Yes</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left gt_striped\" style=\"background-color: #E8F5E9;\">Works on multiple files</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left gt_striped\" style=\"background-color: #E8F5E9;\">One at a time</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left gt_striped\" style=\"background-color: #E8F5E9;\">Yes — all as one table</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left\">Works on a folder</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left\">No</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left\">Yes</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left gt_striped\" style=\"background-color: #E8F5E9;\">Hive partitioning support</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left gt_striped\" style=\"background-color: #E8F5E9;\">No</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left gt_striped\" style=\"background-color: #E8F5E9;\">Yes</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left\" style=\"background-color: #E8F5E9;\">Column pruning</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left\" style=\"background-color: #E8F5E9;\">No — loads all columns</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left\" style=\"background-color: #E8F5E9;\">Yes — loads only needed columns</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left gt_striped\" style=\"background-color: #E8F5E9;\">Partition pruning</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left gt_striped\" style=\"background-color: #E8F5E9;\">No</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left gt_striped\" style=\"background-color: #E8F5E9;\">Yes — skips irrelevant files</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left\" style=\"background-color: #E8F5E9;\">Larger-than-RAM files</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left\" style=\"background-color: #E8F5E9;\">No — will crash</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left\" style=\"background-color: #E8F5E9;\">Yes — streams from disk</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left gt_striped\">dplyr verbs work</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left gt_striped\">Yes — immediately</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left gt_striped\">Yes — lazily until collect()</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left\">collect() required</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left\">No — already a tibble</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left\">Yes — to get a tibble</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left gt_striped\">Schema inspection</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left gt_striped\">After loading</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left gt_striped\">Before loading</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left\">Compression formats</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left\">All Arrow supports</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left\">All Arrow supports</td></tr>\n    <tr><td headers=\"Feature\" class=\"gt_row gt_left gt_striped\">Best for</td>\n<td headers=\"read_parquet\" class=\"gt_row gt_left gt_striped\">Small-medium files</td>\n<td headers=\"open_dataset\" class=\"gt_row gt_left gt_striped\">Large or multiple files</td></tr>\n  </tbody>\n  \n</table>\n</div>\n```\n\n:::\n:::\n\n\n## Similarities — What They Share\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\nlibrary(dplyr)\n\n# ── Both read Parquet format ──────────────────────────────────────────────────\n# Both understand snappy, gzip, zstd, lz4 compression\n# Both read the same column types: string, int32, float64, bool, date, etc.\n\n# ── Both work with dplyr verbs ────────────────────────────────────────────────\n# (open_dataset lazily, read_parquet eagerly)\nresult_rp <- read_parquet(\"data/ga4_flat.parquet\") |>\n  filter(event_name == \"purchase\") |>\n  count(event_date)\n\nresult_od <- open_dataset(\"data/ga4_flat.parquet\") |>\n  filter(event_name == \"purchase\") |>\n  count(event_date) |>\n  collect()\n\n# Results are identical\nidentical(result_rp, result_od)\n#> TRUE\n\n# ── Both support column selection ─────────────────────────────────────────────\n# read Parquet — use col_select to avoid loading unused columns\nga4_small_rp <- read_parquet(\n  \"data/ga4_flat.parquet\",\n  col_select = c(\"event_date\", \"event_name\", \"user_pseudo_id\")\n)\n\n# open_dataset — use select() lazily\nga4_small_od <- open_dataset(\"data/ga4_flat.parquet\") |>\n  select(event_date, event_name, user_pseudo_id) |>\n  collect()\n\n# ── Both can read multiple formats ────────────────────────────────────────────\n# Parquet, Arrow IPC (feather), CSV, JSON, ORC\n\n# read_parquet is Parquet-specific, but open_dataset handles all:\nopen_dataset(\"data/ga4.csv\",    format = \"csv\")\nopen_dataset(\"data/ga4.feather\", format = \"feather\")\nopen_dataset(\"data/ga4.parquet\", format = \"parquet\")  # default\n\n# ── Both support schema inspection ────────────────────────────────────────────\n# read_parquet schema (after loading)\nread_parquet(\"data/ga4_flat.parquet\") |> glimpse()\n\n# open_dataset schema (before loading anything)\nopen_dataset(\"data/ga4_flat.parquet\")$schema\n#> Schema\n#> event_date: string\n#> event_name: string\n#> user_pseudo_id: string\n#> device_category: string\n#> ...\n```\n````\n:::\n\n\n## Practical GA4 Workflows\n\n### Workflow 1: `read_parquet()` for Quick Exploration\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\nlibrary(dplyr)\n\n# You just created a small processed file and want to inspect it\n# File is 15 MB — perfectly fine to load into RAM\n\nga4_items <- read_parquet(\"data/ga4_items.parquet\")\n\n# Now it is just a tibble — use any R function freely\nsummary(ga4_items$price_in_usd)\nhist(ga4_items$quantity)\nView(ga4_items)\n\n# No collect() needed anywhere\nga4_items |>\n  filter(item_category == \"Apparel\") |>\n  arrange(desc(price_in_usd)) |>\n  head(10)\n```\n````\n:::\n\n\n### Workflow 2: `open_dataset()` for Large Event Params\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\nlibrary(dplyr)\n\n# event_params unrolled is very large — open_dataset is safer\nga4_params <- open_dataset(\"data/ga4_event_params.parquet\")\n\n# Check size without loading\nga4_params$schema          # Column names and types\nnrow(ga4_params)           # Row count — reads metadata only, instant\n\n# Query only what you need — Arrow loads only 2 columns from disk\npage_views <- ga4_params |>\n  filter(\n    event_name  == \"page_view\",\n    key         == \"page_location\"\n  ) |>\n  select(event_date, user_pseudo_id, param_value) |>\n  collect()   # Only now does data enter RAM — just the filtered subset\n\nnrow(page_views)   # Much smaller than the full params file\n```\n````\n:::\n\n\n### Workflow 3: `open_dataset()` Across All GA4 Daily Files\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\nlibrary(dplyr)\nlibrary(lubridate)\n\n# GA4 exports one file per day — open_dataset handles all of them\n# Save each day as its own Parquet file first:\n\n# data/ga4_daily/\n#   ga4_20201101.parquet\n#   ga4_20201102.parquet\n#   ... etc\n\n# Open all daily files as one unified dataset\nga4_all <- open_dataset(\n  sources = list.files(\n    \"data/ga4_daily\",\n    pattern    = \"\\\\.parquet$\",\n    full.names = TRUE\n  )\n)\n\ncat(\"Total rows across all files:\", nrow(ga4_all), \"\\n\")\ncat(\"Schema:\\n\")\nprint(ga4_all$schema)\n\n# Query across the full date range — Arrow only reads what is needed\nmonthly_summary <- ga4_all |>\n  mutate(\n    event_date = as.Date(event_date),\n    month      = floor_date(event_date, \"month\")\n  ) |>\n  group_by(month, event_name) |>\n  summarise(\n    events = n(),\n    users  = n_distinct(user_pseudo_id),\n    .groups = \"drop\"\n  ) |>\n  collect()\n\nmonthly_summary |>\n  filter(event_name %in% c(\"page_view\", \"purchase\",\n                             \"add_to_cart\")) |>\n  ggplot2::ggplot(\n    ggplot2::aes(x = month, y = events,\n                  colour = event_name,\n                  group  = event_name)\n  ) +\n  ggplot2::geom_line(linewidth = 1.1) +\n  ggplot2::geom_point(size = 2.5) +\n  ggplot2::scale_y_continuous(labels = scales::comma) +\n  ggplot2::scale_x_date(date_labels = \"%b %Y\") +\n  ggplot2::labs(\n    title  = \"Monthly Event Trends\",\n    x      = NULL,\n    y      = \"Events\",\n    colour = \"Event Type\"\n  ) +\n  ggplot2::theme_minimal(base_size = 13)\n```\n````\n:::\n\n\n### Workflow 4: Combining Both in One Pipeline\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\nlibrary(dplyr)\n\n# Common pattern: open_dataset for the large file, read_parquet for small ones\n# Then join them\n\n# Large file — use open_dataset (lazy)\nga4_params <- open_dataset(\"data/ga4_event_params.parquet\")\n\n# Small lookup file — use read_parquet (eager, it's tiny)\npage_categories <- read_parquet(\"data/page_categories.parquet\")\n# This is a small reference table: page_path → category\n# Fine to have in RAM as a regular tibble\n\n# Extract page locations from the large params file lazily\npage_data <- ga4_params |>\n  filter(key == \"page_location\") |>\n  select(event_date, user_pseudo_id, param_value) |>\n  rename(page_url = param_value) |>\n  mutate(\n    page_path = arrow_string_replace_regex(\n      page_url,\n      \"^https?://[^/]+|\\\\?.*$\", \"\"\n    )\n  ) |>\n  collect()   # Now bring into RAM — filtered to just page views\n\n# Join with the small lookup table (both in RAM now)\npage_data |>\n  left_join(page_categories, by = \"page_path\") |>\n  count(category, sort = TRUE)\n```\n````\n:::\n\n\n### Workflow 5: `open_dataset()` with DuckDB — The Full Stack\n\n\n::: {.cell}\n\n````{.cell-code}\n```{{r}}\n#| eval: false\nlibrary(arrow)\nlibrary(duckdb)\nlibrary(dplyr)\n\n# open_dataset creates Arrow Dataset objects\n# DuckDB can query Arrow objects directly — zero data copying\n\nga4_events_ds <- open_dataset(\"data/ga4_flat.parquet\")\nga4_params_ds <- open_dataset(\"data/ga4_event_params.parquet\")\n\n# Register Arrow datasets with DuckDB\ncon <- dbConnect(duckdb::duckdb())\nduckdb::duckdb_register_arrow(con, \"events\", ga4_events_ds)\nduckdb::duckdb_register_arrow(con, \"params\", ga4_params_ds)\n\n# Now use DuckDB SQL on top of Arrow Datasets\n# Data stays in Arrow format — no copying between systems\nDBI::dbGetQuery(con, \"\n  SELECT\n    e.event_date,\n    e.device_category,\n    COUNT(DISTINCT e.user_pseudo_id)           AS users,\n    SUM(e.ecommerce_purchase_revenue_in_usd)   AS revenue\n  FROM events e\n  WHERE e.event_name = 'purchase'\n  GROUP BY e.event_date, e.device_category\n  ORDER BY e.event_date, revenue DESC\n\")\n\nDBI::dbDisconnect(con, shutdown = TRUE)\n```\n````\n:::\n\n\n## Which to use for GA4 data?\n\n\n```{{r}}\n# Ask these questions in order:\n\n# 1. How big is the file?\n#    < 500 MB and fits in RAM  → read_parquet() is fine\n#    > 500 MB or close to RAM  → open_dataset()\n\n# 2. How many files?\n#    One file                  → either works\n#    Multiple files or folder  → open_dataset() only\n\n# 3. How many columns do you need?\n#    Most or all columns       → read_parquet() is simpler\n#    Only a few columns        → open_dataset() (column pruning saves RAM)\n\n# 4. Are you exploring or producing?\n#    Exploring unknown data    → read_parquet() (View(), summary(), hist())\n#    Building analysis pipeline → open_dataset() (lazy, memory-efficient)\n\n# 5. Do you need the result as a tibble immediately?\n#    Yes                       → read_parquet()\n#    No — you will filter first → open_dataset() + collect()\n```",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {
      "include-in-header": [
        "<link href=\"site_libs/htmltools-fill-0.5.9/fill.css\" rel=\"stylesheet\" />\n<script src=\"site_libs/htmlwidgets-1.6.4/htmlwidgets.js\"></script>\n<script src=\"site_libs/viz-1.8.2/viz.js\"></script>\n<link href=\"site_libs/DiagrammeR-styles-0.2/styles.css\" rel=\"stylesheet\" />\n<script src=\"site_libs/grViz-binding-1.0.11/grViz.js\"></script>\n"
      ]
    },
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}